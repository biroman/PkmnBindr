// src/lib/globalRules.js
// Centralised high-level constants for the whole application.
// Change a value here and it propagates everywhere.

export const GLOBAL_CARD_LIMIT = 1000; // Default maximum cards per binder (update as needed)

export default {
  GLOBAL_CARD_LIMIT,
};
